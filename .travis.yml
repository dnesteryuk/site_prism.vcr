language: ruby
rvm:
  - 2.0.0
  - 1.9.3
matrix:
  allow_failures:
    - rvm: jruby-19mode
    - rvm: rbx-19mode
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script: bundle exec rspec spec
